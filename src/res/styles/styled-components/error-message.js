import styled from 'styled-components';
import { media, styledSystem } from '@utils/styleHelpers';

export const ErrorParagraph = styledSystem(styled.div`
  color: #e0004d;
  font-size: 14px;
  line-height: 18px;
  padding: 3px 0;
`);
